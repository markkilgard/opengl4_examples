
#include <Cg/half.hpp>
#include <Cg/double.hpp>
#include <Cg/vector/xyzw.hpp>
#include <Cg/vector.hpp>
#include <Cg/iostream.hpp>
#include <Cg/dot.hpp>
#include <Cg/clamp.hpp>

using namespace Cg;

#define OUTPUT(_x) std::cout << #_x << "=" << (_x) << std::endl

void test_clamp()
{
  float scalarf = 2.0f;
  float scalari = -6;
  half scalarh = half(3.14159);
  float4 foo = float4(1,2,3,4);
  half4 fooh = half4(1,2,3,4);

  OUTPUT(clamp(foo,foo,foo));
  OUTPUT(clamp(fooh,fooh,fooh));
#ifndef _MSC_VER
  OUTPUT(clamp(foo,foo,fooh));
  OUTPUT(clamp(foo,fooh,foo));
  OUTPUT(clamp(fooh,foo,foo));
  OUTPUT(clamp(4,fooh,5));
  OUTPUT(clamp(4,fooh.x,5));
  OUTPUT(clamp(4,fooh.xy,5));

  OUTPUT(clamp(5,foo,foo.x));
  OUTPUT(clamp(foo,5,foo.x));
  OUTPUT(clamp(5,foo.x,foo));
  OUTPUT(clamp(foo,foo.x,5));
  OUTPUT(clamp(5,foo,foo.x));
  OUTPUT(clamp(foo,5,foo.x));

  OUTPUT(clamp(5,foo.zzw,foo.x));
  OUTPUT(clamp(foo.zzw,5,foo.x));
  OUTPUT(clamp(5,foo.x,foo.zzw));
  OUTPUT(clamp(foo.zzw,foo.x,5));
  OUTPUT(clamp(5,foo.zzw,foo.x));
  OUTPUT(clamp(foo.zzw,5,foo.x));

  OUTPUT(clamp(foo,foo.x,foo));
  OUTPUT(clamp(foo.x,foo,foo.x));

  OUTPUT(clamp(foo.x,foo.x,foo.x));

  OUTPUT(clamp(4,5,fooh.xy));
  OUTPUT(clamp(foo,foo,foo.x));
  OUTPUT(clamp(foo,foo.x,foo.x));
  OUTPUT(clamp(foo.x,foo,foo));
  OUTPUT(clamp(foo.x,foo.x,foo));
  OUTPUT(clamp(foo,foo,foo.x));
  OUTPUT(clamp(foo,foo,fooh.x));
  OUTPUT(clamp(foo.xxy,foo.zzx,foo.x));
  OUTPUT(clamp(foo,5,foo));
  OUTPUT(clamp(foo,foo.x,foo.x));
  OUTPUT(clamp(foo,foo.wzxy,foo.x));
  OUTPUT(clamp(foo,5,foo));
  OUTPUT(clamp(foo,5,foo.wzxy));
  OUTPUT(clamp(foo,5,5));
  OUTPUT(clamp(foo,5,5.5f));
  OUTPUT(clamp(foo,5,5.7));
  OUTPUT(clamp(foo,5,scalarf));
  OUTPUT(clamp(foo,5,scalari));
  OUTPUT(clamp(foo,5,scalarh));
  OUTPUT(clamp(foo,5.5f,foo));
  OUTPUT(clamp(foo,5.5f,foo.wzxy));
  OUTPUT(clamp(foo,5.5f,5));
  OUTPUT(clamp(foo,5.5f,5.5f));
  OUTPUT(clamp(foo,5.5f,5.7));
  OUTPUT(clamp(foo,5.5f,scalarf));
  OUTPUT(clamp(foo,5.5f,scalari));
  OUTPUT(clamp(foo,5.5f,scalarh));
  OUTPUT(clamp(foo,5.7,foo));
  OUTPUT(clamp(foo,5.7,foo.wzxy));
  OUTPUT(clamp(foo,5.7,5));
  OUTPUT(clamp(foo,5.7,5.5f));
  OUTPUT(clamp(foo,5.7,5.7));
  OUTPUT(clamp(foo,5.7,scalarf));
  OUTPUT(clamp(foo,5.7,scalari));
  OUTPUT(clamp(foo,5.7,scalarh));
  OUTPUT(clamp(foo,scalarf,foo));
  OUTPUT(clamp(foo,scalarf,foo.wzxy));
  OUTPUT(clamp(foo,scalarf,5));
  OUTPUT(clamp(foo,scalarf,5.5f));
  OUTPUT(clamp(foo,scalarf,5.7));
  OUTPUT(clamp(foo,scalarf,scalarf));
  OUTPUT(clamp(foo,scalarf,scalari));
  OUTPUT(clamp(foo,scalarf,scalarh));
  OUTPUT(clamp(foo,scalari,foo));
  OUTPUT(clamp(foo,scalari,foo.wzxy));
  OUTPUT(clamp(foo,scalari,5));
  OUTPUT(clamp(foo,scalari,5.5f));
  OUTPUT(clamp(foo,scalari,5.7));
  OUTPUT(clamp(foo,scalari,scalarf));
  OUTPUT(clamp(foo,scalari,scalari));
  OUTPUT(clamp(foo,scalari,scalarh));
  OUTPUT(clamp(foo,scalarh,foo));
  OUTPUT(clamp(foo,foo,foo.xyzw));
  OUTPUT(clamp(foo,foo,scalarf));
  OUTPUT(clamp(foo,foo,5.0f));
  OUTPUT(clamp(foo,foo,5.5f));
  OUTPUT(clamp(foo,foo,5.7));
  OUTPUT(clamp(foo,foo,scalarf));
  OUTPUT(clamp(foo,foo,scalari));
  OUTPUT(clamp(foo,foo,scalarh));
  OUTPUT(clamp(foo,foo.x,foo));
  OUTPUT(clamp(foo,foo.x,foo.wzxy));
  OUTPUT(clamp(foo,foo.x,5));
  OUTPUT(clamp(foo,foo.x,5.5f));
  OUTPUT(clamp(foo,foo.x,5.7));
  OUTPUT(clamp(foo,foo.x,scalarf));
  OUTPUT(clamp(foo,foo.x,scalari));
  OUTPUT(clamp(foo,foo.x,scalarh));
  OUTPUT(clamp(foo,foo.wzxy,foo));
  OUTPUT(clamp(foo,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(foo,foo.wzxy,5));
  OUTPUT(clamp(foo,foo.wzxy,5.5f));
  OUTPUT(clamp(foo,foo.wzxy,5.7));
  OUTPUT(clamp(foo,foo.wzxy,scalarf));
  OUTPUT(clamp(foo,foo.wzxy,scalari));
  OUTPUT(clamp(foo,foo.wzxy,scalarh));
  OUTPUT(clamp(foo,5,foo.x));
  OUTPUT(clamp(foo,5.5f,foo.x));
  OUTPUT(clamp(foo,5.7,foo.x));
  OUTPUT(clamp(foo,scalarf,foo.x));
  OUTPUT(clamp(foo,scalari,foo.x));
  OUTPUT(clamp(foo,scalarh,foo.x));
  OUTPUT(clamp(foo,scalarh,foo.wzxy));
  OUTPUT(clamp(foo,scalarh,5));
  OUTPUT(clamp(foo,scalarh,5.5f));
  OUTPUT(clamp(foo,scalarh,5.7));
  OUTPUT(clamp(foo,scalarh,scalarf));
  OUTPUT(clamp(foo,scalarh,scalari));
  OUTPUT(clamp(foo,scalarh,scalarh));
  OUTPUT(clamp(foo.x,foo,foo));
  OUTPUT(clamp(foo.x,foo,foo.x));
  OUTPUT(clamp(foo.x,foo,foo.wzxy));
  OUTPUT(clamp(foo.x,foo,5));
  OUTPUT(clamp(foo.x,foo,5.5f));
  OUTPUT(clamp(foo.x,foo,5.7));
  OUTPUT(clamp(foo.x,foo,scalarf));
  OUTPUT(clamp(foo.x,foo,scalari));
  OUTPUT(clamp(foo.x,foo,scalarh));
  OUTPUT(clamp(foo.x,foo.x,foo));
  OUTPUT(clamp(float1(foo.x),foo.x,foo.x)); // ambigugous
  OUTPUT(clamp(foo.x,foo.x,foo.wzxy));
  OUTPUT(clamp(float1(foo.x),foo.x,5)); // ambigugous
  OUTPUT(clamp(foo.x,float1(foo.x),5.5f)); // ambigugous
  OUTPUT(clamp(foo.x,foo.x,float1(5.7f)));  // ambigugous
  OUTPUT(clamp(foo.x,float1(foo.x),scalarf));
  OUTPUT(clamp(foo.x,float1(foo.x),scalari));
  OUTPUT(clamp(foo.x,float1(foo.x),scalarh));
  OUTPUT(clamp(foo.x,foo.wzxy,foo));
  OUTPUT(clamp(foo.x,foo.wzxy,foo.x));
  OUTPUT(clamp(foo.x,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(foo.x,foo.wzxy,5));
  OUTPUT(clamp(foo.x,foo.wzxy,5.5f));
  OUTPUT(clamp(foo.x,foo.wzxy,5.7));
  OUTPUT(clamp(foo.x,foo.wzxy,scalarf));
  OUTPUT(clamp(foo.x,foo.wzxy,scalari));
  OUTPUT(clamp(foo.x,foo.wzxy,scalarh));
  OUTPUT(clamp(foo.x,5,foo));
  OUTPUT(clamp(foo.x,5,float1(foo.x)));
  OUTPUT(clamp(foo.x,5,foo.wzxy));
  OUTPUT(clamp(float1(foo.x),5,5));
  OUTPUT(clamp(float1(foo.x),5,5.5f));
  OUTPUT(clamp(foo.x,float1(5),5.7));
  OUTPUT(clamp(foo.x,float1(5),scalarf));
  OUTPUT(clamp(foo.x,float1(5),scalari));
  OUTPUT(clamp(foo.x,5,float1(scalarh)));
  OUTPUT(clamp(foo.x,5.5f,foo));
  OUTPUT(clamp(float1(foo.x),5.5f,foo.x));
  OUTPUT(clamp(foo.x,5.5f,foo.wzxy));
  OUTPUT(clamp(float1(foo.x),5.5f,5));
  OUTPUT(clamp(float1(foo.x),5.5f,5.5f));
  OUTPUT(clamp(float1(foo.x),5.5f,5.7));
  OUTPUT(clamp(float1(foo.x),5.5f,scalarf));
  OUTPUT(clamp(foo.x,5.5f,float1(scalari)));
  OUTPUT(clamp(foo.x,5.5f,float1(scalarh)));
  OUTPUT(clamp(foo.x,5.7,foo));
  OUTPUT(clamp(foo.x,5.7,foo.x));
  OUTPUT(clamp(foo.x,5.7,foo.wzxy));
  OUTPUT(clamp(foo.x,5.7,5));
  OUTPUT(clamp(foo.x,5.7,5.5f));
  OUTPUT(clamp(foo.x,5.7,5.7));
  OUTPUT(clamp(foo.x,5.7,scalarf));
  OUTPUT(clamp(foo.x,5.7,scalari));
  OUTPUT(clamp(foo.x,5.7,scalarh));
  OUTPUT(clamp(foo.x,scalarf,foo));
  OUTPUT(clamp(foo.x,scalarf,foo.x));
  OUTPUT(clamp(foo.x,scalarf,foo.wzxy));
  OUTPUT(clamp(foo.x,scalarf,5));
  OUTPUT(clamp(foo.x,scalarf,5.5f));
  OUTPUT(clamp(foo.x,scalarf,5.7));
  OUTPUT(clamp(foo.x,scalarf,scalarf));
  OUTPUT(clamp(foo.x,scalarf,scalari));
  OUTPUT(clamp(foo.x,scalarf,scalarh));
  OUTPUT(clamp(foo.x,scalari,foo));
  OUTPUT(clamp(foo.x,scalari,foo.x));
  OUTPUT(clamp(foo.x,scalari,foo.wzxy));
  OUTPUT(clamp(foo.x,scalari,5));
  OUTPUT(clamp(foo.x,scalari,5.5f));
  OUTPUT(clamp(foo.x,scalari,5.7));
  OUTPUT(clamp(foo.x,scalari,scalarf));
  OUTPUT(clamp(foo.x,scalari,scalari));
  OUTPUT(clamp(foo.x,scalari,scalarh));
  OUTPUT(clamp(foo.x,scalarh,foo));
  OUTPUT(clamp(foo.x,scalarh,foo.x));
  OUTPUT(clamp(foo.x,scalarh,foo.wzxy));
  OUTPUT(clamp(foo.x,scalarh,5));
  OUTPUT(clamp(foo.x,scalarh,5.5f));
  OUTPUT(clamp(foo.x,scalarh,5.7));
  OUTPUT(clamp(foo.x,scalarh,scalarf));
  OUTPUT(clamp(foo.x,scalarh,scalari));
  OUTPUT(clamp(foo.x,scalarh,scalarh));
  OUTPUT(clamp(foo.wzxy,foo,foo));
  OUTPUT(clamp(foo.wzxy,foo,foo.x));
  OUTPUT(clamp(foo.wzxy,foo,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,foo,5));
  OUTPUT(clamp(foo.wzxy,foo,5.5f));
  OUTPUT(clamp(foo.wzxy,foo,5.7));
  OUTPUT(clamp(foo.wzxy,foo,scalarf));
  OUTPUT(clamp(foo.wzxy,foo,scalari));
  OUTPUT(clamp(foo.wzxy,foo,scalarh));
  OUTPUT(clamp(foo.wzxy,foo.x,foo));
  OUTPUT(clamp(foo.wzxy,foo.x,foo.x));
  OUTPUT(clamp(foo.wzxy,foo.x,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,foo.x,5));
  OUTPUT(clamp(foo.wzxy,foo.x,5.5f));
  OUTPUT(clamp(foo.wzxy,foo.x,5.7));
  OUTPUT(clamp(foo.wzxy,foo.x,scalarf));
  OUTPUT(clamp(foo.wzxy,foo.x,scalari));
  OUTPUT(clamp(foo.wzxy,foo.x,scalarh));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,foo));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,foo.x));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,5));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,5.5f));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,5.7));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,scalarf));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,scalari));
  OUTPUT(clamp(foo.wzxy,foo.wzxy,scalarh));
  OUTPUT(clamp(foo.wzxy,5,foo));
  OUTPUT(clamp(foo.wzxy,5,foo.x));
  OUTPUT(clamp(foo.wzxy,5,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,5,5));
  OUTPUT(clamp(foo.wzxy,5,5.5f));
  OUTPUT(clamp(foo.wzxy,5,5.7));
  OUTPUT(clamp(foo.wzxy,5,scalarf));
  OUTPUT(clamp(foo.wzxy,5,scalari));
  OUTPUT(clamp(foo.wzxy,5,scalarh));
  OUTPUT(clamp(foo.wzxy,5.5f,foo));
  OUTPUT(clamp(foo.wzxy,5.5f,foo.x));
  OUTPUT(clamp(foo.wzxy,5.5f,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,5.5f,5));
  OUTPUT(clamp(foo.wzxy,5.5f,5.5f));
  OUTPUT(clamp(foo.wzxy,5.5f,5.7));
  OUTPUT(clamp(foo.wzxy,5.5f,scalarf));
  OUTPUT(clamp(foo.wzxy,5.5f,scalari));
  OUTPUT(clamp(foo.wzxy,5.5f,scalarh));
  OUTPUT(clamp(foo.wzxy,5.7,foo));
  OUTPUT(clamp(foo.wzxy,5.7,foo.x));
  OUTPUT(clamp(foo.wzxy,5.7,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,5.7,5));
  OUTPUT(clamp(foo.wzxy,5.7,5.5f));
  OUTPUT(clamp(foo.wzxy,5.7,5.7));
  OUTPUT(clamp(foo.wzxy,5.7,scalarf));
  OUTPUT(clamp(foo.wzxy,5.7,scalari));
  OUTPUT(clamp(foo.wzxy,5.7,scalarh));
  OUTPUT(clamp(foo.wzxy,scalarf,foo));
  OUTPUT(clamp(foo.wzxy,scalarf,foo.x));
  OUTPUT(clamp(foo.wzxy,scalarf,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,scalarf,5));
  OUTPUT(clamp(foo.wzxy,scalarf,5.5f));
  OUTPUT(clamp(foo.wzxy,scalarf,5.7));
  OUTPUT(clamp(foo.wzxy,scalarf,scalarf));
  OUTPUT(clamp(foo.wzxy,scalarf,scalari));
  OUTPUT(clamp(foo.wzxy,scalarf,scalarh));
  OUTPUT(clamp(foo.wzxy,scalari,foo));
  OUTPUT(clamp(foo.wzxy,scalari,foo.x));
  OUTPUT(clamp(foo.wzxy,scalari,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,scalari,5));
  OUTPUT(clamp(foo.wzxy,scalari,5.5f));
  OUTPUT(clamp(foo.wzxy,scalari,5.7));
  OUTPUT(clamp(foo.wzxy,scalari,scalarf));
  OUTPUT(clamp(foo.wzxy,scalari,scalari));
  OUTPUT(clamp(foo.wzxy,scalari,scalarh));
  OUTPUT(clamp(foo.wzxy,scalarh,foo));
  OUTPUT(clamp(foo.wzxy,scalarh,foo.x));
  OUTPUT(clamp(foo.wzxy,scalarh,foo.wzxy));
  OUTPUT(clamp(foo.wzxy,scalarh,5));
  OUTPUT(clamp(foo.wzxy,scalarh,5.5f));
  OUTPUT(clamp(foo.wzxy,scalarh,5.7));
  OUTPUT(clamp(foo.wzxy,scalarh,scalarf));
  OUTPUT(clamp(foo.wzxy,scalarh,scalari));
  OUTPUT(clamp(foo.wzxy,scalarh,scalarh));
  OUTPUT(clamp(5,foo,foo));
  OUTPUT(clamp(5,foo,foo.x));
  OUTPUT(clamp(5,foo,foo.wzxy));
  OUTPUT(clamp(5,foo,5));
  OUTPUT(clamp(5,foo,5.5f));
  OUTPUT(clamp(5,foo,5.7));
  OUTPUT(clamp(5,foo,scalarf));
  OUTPUT(clamp(5,foo,scalari));
  OUTPUT(clamp(5,foo,scalarh));
  OUTPUT(clamp(5,foo.x,foo));
  OUTPUT(clamp(5,foo.x,foo.x));
  OUTPUT(clamp(5,foo.x,foo.wzxy));
  OUTPUT(clamp(5,foo.x,5));
  OUTPUT(clamp(5,foo.x,5.5f));
  OUTPUT(clamp(5,foo.x,5.7));
  OUTPUT(clamp(5,foo.x,scalarf));
  OUTPUT(clamp(5,foo.x,scalari));
  OUTPUT(clamp(5,foo.x,scalarh));
  OUTPUT(clamp(5,foo.wzxy,foo));
  OUTPUT(clamp(5,foo.wzxy,foo.x));
  OUTPUT(clamp(5,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(5,foo.wzxy,5));
  OUTPUT(clamp(5,foo.wzxy,5.5f));
  OUTPUT(clamp(5,foo.wzxy,5.7));
  OUTPUT(clamp(5,foo.wzxy,scalarf));
  OUTPUT(clamp(5,foo.wzxy,scalari));
  OUTPUT(clamp(5,foo.wzxy,scalarh));
  OUTPUT(clamp(5,5,foo));
  OUTPUT(clamp(5,5,foo.x));
  OUTPUT(clamp(5,5,foo.wzxy));
  OUTPUT(clamp(5,5,5));
  OUTPUT(clamp(5,5,5.5f));
  OUTPUT(clamp(5,5,5.7));
  OUTPUT(clamp(5,5,scalarf));
  OUTPUT(clamp(5,5,scalari));
  OUTPUT(clamp(5,5,scalarh));
  OUTPUT(clamp(5,5.5f,foo));
  OUTPUT(clamp(5,5.5f,foo.x));
  OUTPUT(clamp(5,5.5f,foo.wzxy));
  OUTPUT(clamp(5,5.5f,5));
  OUTPUT(clamp(5,5.5f,5.5f));
  OUTPUT(clamp(5,5.5f,5.7));
  OUTPUT(clamp(5,5.5f,scalarf));
  OUTPUT(clamp(5,5.5f,scalari));
  OUTPUT(clamp(5,5.5f,scalarh));
  OUTPUT(clamp(5,5.7,foo));
  OUTPUT(clamp(5,5.7,foo.x));
  OUTPUT(clamp(5,5.7,foo.wzxy));
  OUTPUT(clamp(5,5.7,5));
  OUTPUT(clamp(5,5.7,5.5f));
  OUTPUT(clamp(5,5.7,5.7));
  OUTPUT(clamp(5,5.7,scalarf));
  OUTPUT(clamp(5,5.7,scalari));
  OUTPUT(clamp(5,5.7,scalarh));
  OUTPUT(clamp(5,scalarf,foo));
  OUTPUT(clamp(5,scalarf,foo.x));
  OUTPUT(clamp(5,scalarf,foo.wzxy));
  OUTPUT(clamp(5,scalarf,5));
  OUTPUT(clamp(5,scalarf,5.5f));
  OUTPUT(clamp(5,scalarf,5.7));
  OUTPUT(clamp(5,scalarf,scalarf));
  OUTPUT(clamp(5,scalarf,scalari));
  OUTPUT(clamp(5,scalarf,scalarh));
  OUTPUT(clamp(5,scalari,foo));
  OUTPUT(clamp(5,scalari,foo.x));
  OUTPUT(clamp(5,scalari,foo.wzxy));
  OUTPUT(clamp(5,scalari,5));
  OUTPUT(clamp(5,scalari,5.5f));
  OUTPUT(clamp(5,scalari,5.7));
  OUTPUT(clamp(5,scalari,scalarf));
  OUTPUT(clamp(5,scalari,scalari));
  OUTPUT(clamp(5,scalari,scalarh));
  OUTPUT(clamp(5,scalarh,foo));
  OUTPUT(clamp(5,scalarh,foo.x));
  OUTPUT(clamp(5,scalarh,foo.wzxy));
  OUTPUT(clamp(5,scalarh,5));
  OUTPUT(clamp(5,scalarh,5.5f));
  OUTPUT(clamp(5,scalarh,5.7));
  OUTPUT(clamp(5,scalarh,scalarf));
  OUTPUT(clamp(5,scalarh,scalari));
  OUTPUT(clamp(5,scalarh,scalarh));
  OUTPUT(clamp(5.5f,foo,foo));
  OUTPUT(clamp(5.5f,foo,foo.x));
  OUTPUT(clamp(5.5f,foo,foo.wzxy));
  OUTPUT(clamp(5.5f,foo,5));
  OUTPUT(clamp(5.5f,foo,5.5f));
  OUTPUT(clamp(5.5f,foo,5.7));
  OUTPUT(clamp(5.5f,foo,scalarf));
  OUTPUT(clamp(5.5f,foo,scalari));
  OUTPUT(clamp(5.5f,foo,scalarh));
  OUTPUT(clamp(5.5f,foo.x,foo));
  OUTPUT(clamp(5.5f,foo.x,foo.x));
  OUTPUT(clamp(5.5f,foo.x,foo.wzxy));
  OUTPUT(clamp(5.5f,foo.x,5));
  OUTPUT(clamp(5.5f,foo.x,5.5f));
  OUTPUT(clamp(5.5f,foo.x,5.7));
  OUTPUT(clamp(5.5f,foo.x,scalarf));
  OUTPUT(clamp(5.5f,foo.x,scalari));
  OUTPUT(clamp(5.5f,foo.x,scalarh));
  OUTPUT(clamp(5.5f,foo.wzxy,foo));
  OUTPUT(clamp(5.5f,foo.wzxy,foo.x));
  OUTPUT(clamp(5.5f,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(5.5f,foo.wzxy,5));
  OUTPUT(clamp(5.5f,foo.wzxy,5.5f));
  OUTPUT(clamp(5.5f,foo.wzxy,5.7));
  OUTPUT(clamp(5.5f,foo.wzxy,scalarf));
  OUTPUT(clamp(5.5f,foo.wzxy,scalari));
  OUTPUT(clamp(5.5f,foo.wzxy,scalarh));
  OUTPUT(clamp(5.5f,5,foo));
  OUTPUT(clamp(5.5f,5,foo.x));
  OUTPUT(clamp(5.5f,5,foo.wzxy));
  OUTPUT(clamp(5.5f,5,5));
  OUTPUT(clamp(5.5f,5,5.5f));
  OUTPUT(clamp(5.5f,5,5.7));
  OUTPUT(clamp(5.5f,5,scalarf));
  OUTPUT(clamp(5.5f,5,scalari));
  OUTPUT(clamp(5.5f,5,scalarh));
  OUTPUT(clamp(5.5f,5.5f,foo));
  OUTPUT(clamp(5.5f,5.5f,foo.x));
  OUTPUT(clamp(5.5f,5.5f,foo.wzxy));
  OUTPUT(clamp(5.5f,5.5f,5));
  OUTPUT(clamp(5.5f,5.5f,5.5f));
  OUTPUT(clamp(5.5f,5.5f,5.7));
  OUTPUT(clamp(5.5f,5.5f,scalarf));
  OUTPUT(clamp(5.5f,5.5f,scalari));
  OUTPUT(clamp(5.5f,5.5f,scalarh));
  OUTPUT(clamp(5.5f,5.7,foo));
  OUTPUT(clamp(5.5f,5.7,foo.x));
  OUTPUT(clamp(5.5f,5.7,foo.wzxy));
  OUTPUT(clamp(5.5f,5.7,5));
  OUTPUT(clamp(5.5f,5.7,5.5f));
  OUTPUT(clamp(5.5f,5.7,5.7));
  OUTPUT(clamp(5.5f,5.7,scalarf));
  OUTPUT(clamp(5.5f,5.7,scalari));
  OUTPUT(clamp(5.5f,5.7,scalarh));
  OUTPUT(clamp(5.5f,scalarf,foo));
  OUTPUT(clamp(5.5f,scalarf,foo.x));
  OUTPUT(clamp(5.5f,scalarf,foo.wzxy));
  OUTPUT(clamp(5.5f,scalarf,5));
  OUTPUT(clamp(5.5f,scalarf,5.5f));
  OUTPUT(clamp(5.5f,scalarf,5.7));
  OUTPUT(clamp(5.5f,scalarf,scalarf));
  OUTPUT(clamp(5.5f,scalarf,scalari));
  OUTPUT(clamp(5.5f,scalarf,scalarh));
  OUTPUT(clamp(5.5f,scalari,foo));
  OUTPUT(clamp(5.5f,scalari,foo.x));
  OUTPUT(clamp(5.5f,scalari,foo.wzxy));
  OUTPUT(clamp(5.5f,scalari,5));
  OUTPUT(clamp(5.5f,scalari,5.5f));
  OUTPUT(clamp(5.5f,scalari,5.7));
  OUTPUT(clamp(5.5f,scalari,scalarf));
  OUTPUT(clamp(5.5f,scalari,scalari));
  OUTPUT(clamp(5.5f,scalari,scalarh));
  OUTPUT(clamp(5.5f,scalarh,foo));
  OUTPUT(clamp(5.5f,scalarh,foo.x));
  OUTPUT(clamp(5.5f,scalarh,foo.wzxy));
  OUTPUT(clamp(5.5f,scalarh,5));
  OUTPUT(clamp(5.5f,scalarh,5.5f));
  OUTPUT(clamp(5.5f,scalarh,5.7));
  OUTPUT(clamp(5.5f,scalarh,scalarf));
  OUTPUT(clamp(5.5f,scalarh,scalari));
  OUTPUT(clamp(5.5f,scalarh,scalarh));
  OUTPUT(clamp(5.7,foo,foo));
  OUTPUT(clamp(5.7,foo,foo.x));
  OUTPUT(clamp(5.7,foo,foo.wzxy));
  OUTPUT(clamp(5.7,foo,5));
  OUTPUT(clamp(5.7,foo,5.5f));
  OUTPUT(clamp(5.7,foo,5.7));
  OUTPUT(clamp(5.7,foo,scalarf));
  OUTPUT(clamp(5.7,foo,scalari));
  OUTPUT(clamp(5.7,foo,scalarh));
  OUTPUT(clamp(5.7,foo.x,foo));
  OUTPUT(clamp(5.7,foo.x,foo.x));
  OUTPUT(clamp(5.7,foo.x,foo.wzxy));
  OUTPUT(clamp(5.7,foo.x,5));
  OUTPUT(clamp(5.7,foo.x,5.5f));
  OUTPUT(clamp(5.7,foo.x,5.7));
  OUTPUT(clamp(5.7,foo.x,scalarf));
  OUTPUT(clamp(5.7,foo.x,scalari));
  OUTPUT(clamp(5.7,foo.x,scalarh));
  OUTPUT(clamp(5.7,foo.wzxy,foo));
  OUTPUT(clamp(5.7,foo.wzxy,foo.x));
  OUTPUT(clamp(5.7,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(5.7,foo.wzxy,5));
  OUTPUT(clamp(5.7,foo.wzxy,5.5f));
  OUTPUT(clamp(5.7,foo.wzxy,5.7));
  OUTPUT(clamp(5.7,foo.wzxy,scalarf));
  OUTPUT(clamp(5.7,foo.wzxy,scalari));
  OUTPUT(clamp(5.7,foo.wzxy,scalarh));
  OUTPUT(clamp(5.7,5,foo));
  OUTPUT(clamp(5.7,5,foo.x));
  OUTPUT(clamp(5.7,5,foo.wzxy));
  OUTPUT(clamp(5.7,5,5));
  OUTPUT(clamp(5.7,5,5.5f));
  OUTPUT(clamp(5.7,5,5.7));
  OUTPUT(clamp(5.7,5,scalarf));
  OUTPUT(clamp(5.7,5,scalari));
  OUTPUT(clamp(5.7,5,scalarh));
  OUTPUT(clamp(5.7,5.5f,foo));
  OUTPUT(clamp(5.7,5.5f,foo.x));
  OUTPUT(clamp(5.7,5.5f,foo.wzxy));
  OUTPUT(clamp(5.7,5.5f,5));
  OUTPUT(clamp(5.7,5.5f,5.5f));
  OUTPUT(clamp(5.7,5.5f,5.7));
  OUTPUT(clamp(5.7,5.5f,scalarf));
  OUTPUT(clamp(5.7,5.5f,scalari));
  OUTPUT(clamp(5.7,5.5f,scalarh));
  OUTPUT(clamp(5.7,5.7,foo));
  OUTPUT(clamp(5.7,5.7,foo.x));
  OUTPUT(clamp(5.7,5.7,foo.wzxy));
  OUTPUT(clamp(5.7,5.7,5));
  OUTPUT(clamp(5.7,5.7,5.5f));
  OUTPUT(clamp(5.7,5.7,5.7));
  OUTPUT(clamp(5.7,5.7,scalarf));
  OUTPUT(clamp(5.7,5.7,scalari));
  OUTPUT(clamp(5.7,5.7,scalarh));
  OUTPUT(clamp(5.7,scalarf,foo));
  OUTPUT(clamp(5.7,scalarf,foo.x));
  OUTPUT(clamp(5.7,scalarf,foo.wzxy));
  OUTPUT(clamp(5.7,scalarf,5));
  OUTPUT(clamp(5.7,scalarf,5.5f));
  OUTPUT(clamp(5.7,scalarf,5.7));
  OUTPUT(clamp(5.7,scalarf,scalarf));
  OUTPUT(clamp(5.7,scalarf,scalari));
  OUTPUT(clamp(5.7,scalarf,scalarh));
  OUTPUT(clamp(5.7,scalari,foo));
  OUTPUT(clamp(5.7,scalari,foo.x));
  OUTPUT(clamp(5.7,scalari,foo.wzxy));
  OUTPUT(clamp(5.7,scalari,5));
  OUTPUT(clamp(5.7,scalari,5.5f));
  OUTPUT(clamp(5.7,scalari,5.7));
  OUTPUT(clamp(5.7,scalari,scalarf));
  OUTPUT(clamp(5.7,scalari,scalari));
  OUTPUT(clamp(5.7,scalari,scalarh));
  OUTPUT(clamp(5.7,scalarh,foo));
  OUTPUT(clamp(5.7,scalarh,foo.x));
  OUTPUT(clamp(5.7,scalarh,foo.wzxy));
  OUTPUT(clamp(5.7,scalarh,5));
  OUTPUT(clamp(5.7,scalarh,5.5f));
  OUTPUT(clamp(5.7,scalarh,5.7));
  OUTPUT(clamp(5.7,scalarh,scalarf));
  OUTPUT(clamp(5.7,scalarh,scalari));
  OUTPUT(clamp(5.7,scalarh,scalarh));
  OUTPUT(clamp(scalarf,foo,foo));
  OUTPUT(clamp(scalarf,foo,foo.x));
  OUTPUT(clamp(scalarf,foo,foo.wzxy));
  OUTPUT(clamp(scalarf,foo,5));
  OUTPUT(clamp(scalarf,foo,5.5f));
  OUTPUT(clamp(scalarf,foo,5.7));
  OUTPUT(clamp(scalarf,foo,scalarf));
  OUTPUT(clamp(scalarf,foo,scalari));
  OUTPUT(clamp(scalarf,foo,scalarh));
  OUTPUT(clamp(scalarf,foo.x,foo));
  OUTPUT(clamp(scalarf,foo.x,foo.x));
  OUTPUT(clamp(scalarf,foo.x,foo.wzxy));
  OUTPUT(clamp(scalarf,foo.x,5));
  OUTPUT(clamp(scalarf,foo.x,5.5f));
  OUTPUT(clamp(scalarf,foo.x,5.7));
  OUTPUT(clamp(scalarf,foo.x,scalarf));
  OUTPUT(clamp(scalarf,foo.x,scalari));
  OUTPUT(clamp(scalarf,foo.x,scalarh));
  OUTPUT(clamp(scalarf,foo.wzxy,foo));
  OUTPUT(clamp(scalarf,foo.wzxy,foo.x));
  OUTPUT(clamp(scalarf,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(scalarf,foo.wzxy,5));
  OUTPUT(clamp(scalarf,foo.wzxy,5.5f));
  OUTPUT(clamp(scalarf,foo.wzxy,5.7));
  OUTPUT(clamp(scalarf,foo.wzxy,scalarf));
  OUTPUT(clamp(scalarf,foo.wzxy,scalari));
  OUTPUT(clamp(scalarf,foo.wzxy,scalarh));
  OUTPUT(clamp(scalarf,5,foo));
  OUTPUT(clamp(scalarf,5,foo.x));
  OUTPUT(clamp(scalarf,5,foo.wzxy));
  OUTPUT(clamp(scalarf,5,5));
  OUTPUT(clamp(scalarf,5,5.5f));
  OUTPUT(clamp(scalarf,5,5.7));
  OUTPUT(clamp(scalarf,5,scalarf));
  OUTPUT(clamp(scalarf,5,scalari));
  OUTPUT(clamp(scalarf,5,scalarh));
  OUTPUT(clamp(scalarf,5.5f,foo));
  OUTPUT(clamp(scalarf,5.5f,foo.x));
  OUTPUT(clamp(scalarf,5.5f,foo.wzxy));
  OUTPUT(clamp(scalarf,5.5f,5));
  OUTPUT(clamp(scalarf,5.5f,5.5f));
  OUTPUT(clamp(scalarf,5.5f,5.7));
  OUTPUT(clamp(scalarf,5.5f,scalarf));
  OUTPUT(clamp(scalarf,5.5f,scalari));
  OUTPUT(clamp(scalarf,5.5f,scalarh));
  OUTPUT(clamp(scalarf,5.7,foo));
  OUTPUT(clamp(scalarf,5.7,foo.x));
  OUTPUT(clamp(scalarf,5.7,foo.wzxy));
  OUTPUT(clamp(scalarf,5.7,5));
  OUTPUT(clamp(scalarf,5.7,5.5f));
  OUTPUT(clamp(scalarf,5.7,5.7));
  OUTPUT(clamp(scalarf,5.7,scalarf));
  OUTPUT(clamp(scalarf,5.7,scalari));
  OUTPUT(clamp(scalarf,5.7,scalarh));
  OUTPUT(clamp(scalarf,scalarf,foo));
  OUTPUT(clamp(scalarf,scalarf,foo.x));
  OUTPUT(clamp(scalarf,scalarf,foo.wzxy));
  OUTPUT(clamp(scalarf,scalarf,5));
  OUTPUT(clamp(scalarf,scalarf,5.5f));
  OUTPUT(clamp(scalarf,scalarf,5.7));
  OUTPUT(clamp(scalarf,scalarf,scalarf));
  OUTPUT(clamp(scalarf,scalarf,scalari));
  OUTPUT(clamp(scalarf,scalarf,scalarh));
  OUTPUT(clamp(scalarf,scalari,foo));
  OUTPUT(clamp(scalarf,scalari,foo.x));
  OUTPUT(clamp(scalarf,scalari,foo.wzxy));
  OUTPUT(clamp(scalarf,scalari,5));
  OUTPUT(clamp(scalarf,scalari,5.5f));
  OUTPUT(clamp(scalarf,scalari,5.7));
  OUTPUT(clamp(scalarf,scalari,scalarf));
  OUTPUT(clamp(scalarf,scalari,scalari));
  OUTPUT(clamp(scalarf,scalari,scalarh));
  OUTPUT(clamp(scalarf,scalarh,foo));
  OUTPUT(clamp(scalarf,scalarh,foo.x));
  OUTPUT(clamp(scalarf,scalarh,foo.wzxy));
  OUTPUT(clamp(scalarf,scalarh,5));
  OUTPUT(clamp(scalarf,scalarh,5.5f));
  OUTPUT(clamp(scalarf,scalarh,5.7));
  OUTPUT(clamp(scalarf,scalarh,scalarf));
  OUTPUT(clamp(scalarf,scalarh,scalari));
  OUTPUT(clamp(scalarf,scalarh,scalarh));
  OUTPUT(clamp(scalari,foo,foo));
  OUTPUT(clamp(scalari,foo,foo.x));
  OUTPUT(clamp(scalari,foo,foo.wzxy));
  OUTPUT(clamp(scalari,foo,5));
  OUTPUT(clamp(scalari,foo,5.5f));
  OUTPUT(clamp(scalari,foo,5.7));
  OUTPUT(clamp(scalari,foo,scalarf));
  OUTPUT(clamp(scalari,foo,scalari));
  OUTPUT(clamp(scalari,foo,scalarh));
  OUTPUT(clamp(scalari,foo.x,foo));
  OUTPUT(clamp(scalari,foo.x,foo.x));
  OUTPUT(clamp(scalari,foo.x,foo.wzxy));
  OUTPUT(clamp(scalari,foo.x,5));
  OUTPUT(clamp(scalari,foo.x,5.5f));
  OUTPUT(clamp(scalari,foo.x,5.7));
  OUTPUT(clamp(scalari,foo.x,scalarf));
  OUTPUT(clamp(scalari,foo.x,scalari));
  OUTPUT(clamp(scalari,foo.x,scalarh));
  OUTPUT(clamp(scalari,foo.wzxy,foo));
  OUTPUT(clamp(scalari,foo.wzxy,foo.x));
  OUTPUT(clamp(scalari,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(scalari,foo.wzxy,5));
  OUTPUT(clamp(scalari,foo.wzxy,5.5f));
  OUTPUT(clamp(scalari,foo.wzxy,5.7));
  OUTPUT(clamp(scalari,foo.wzxy,scalarf));
  OUTPUT(clamp(scalari,foo.wzxy,scalari));
  OUTPUT(clamp(scalari,foo.wzxy,scalarh));
  OUTPUT(clamp(scalari,5,foo));
  OUTPUT(clamp(scalari,5,foo.x));
  OUTPUT(clamp(scalari,5,foo.wzxy));
  OUTPUT(clamp(scalari,5,5));
  OUTPUT(clamp(scalari,5,5.5f));
  OUTPUT(clamp(scalari,5,5.7));
  OUTPUT(clamp(scalari,5,scalarf));
  OUTPUT(clamp(scalari,5,scalari));
  OUTPUT(clamp(scalari,5,scalarh));
  OUTPUT(clamp(scalari,5.5f,foo));
  OUTPUT(clamp(scalari,5.5f,foo.x));
  OUTPUT(clamp(scalari,5.5f,foo.wzxy));
  OUTPUT(clamp(scalari,5.5f,5));
  OUTPUT(clamp(scalari,5.5f,5.5f));
  OUTPUT(clamp(scalari,5.5f,5.7));
  OUTPUT(clamp(scalari,5.5f,scalarf));
  OUTPUT(clamp(scalari,5.5f,scalari));
  OUTPUT(clamp(scalari,5.5f,scalarh));
  OUTPUT(clamp(scalari,5.7,foo));
  OUTPUT(clamp(scalari,5.7,foo.x));
  OUTPUT(clamp(scalari,5.7,foo.wzxy));
  OUTPUT(clamp(scalari,5.7,5));
  OUTPUT(clamp(scalari,5.7,5.5f));
  OUTPUT(clamp(scalari,5.7,5.7));
  OUTPUT(clamp(scalari,5.7,scalarf));
  OUTPUT(clamp(scalari,5.7,scalari));
  OUTPUT(clamp(scalari,5.7,scalarh));
  OUTPUT(clamp(scalari,scalarf,foo));
  OUTPUT(clamp(scalari,scalarf,foo.x));
  OUTPUT(clamp(scalari,scalarf,foo.wzxy));
  OUTPUT(clamp(scalari,scalarf,5));
  OUTPUT(clamp(scalari,scalarf,5.5f));
  OUTPUT(clamp(scalari,scalarf,5.7));
  OUTPUT(clamp(scalari,scalarf,scalarf));
  OUTPUT(clamp(scalari,scalarf,scalari));
  OUTPUT(clamp(scalari,scalarf,scalarh));
  OUTPUT(clamp(scalari,scalari,foo));
  OUTPUT(clamp(scalari,scalari,foo.x));
  OUTPUT(clamp(scalari,scalari,foo.wzxy));
  OUTPUT(clamp(scalari,scalari,5));
  OUTPUT(clamp(scalari,scalari,5.5f));
  OUTPUT(clamp(scalari,scalari,5.7));
  OUTPUT(clamp(scalari,scalari,scalarf));
  OUTPUT(clamp(scalari,scalari,scalari));
  OUTPUT(clamp(scalari,scalari,scalarh));
  OUTPUT(clamp(scalari,scalarh,foo));
  OUTPUT(clamp(scalari,scalarh,foo.x));
  OUTPUT(clamp(scalari,scalarh,foo.wzxy));
  OUTPUT(clamp(scalari,scalarh,5));
  OUTPUT(clamp(scalari,scalarh,5.5f));
  OUTPUT(clamp(scalari,scalarh,5.7));
  OUTPUT(clamp(scalari,scalarh,scalarf));
  OUTPUT(clamp(scalari,scalarh,scalari));
  OUTPUT(clamp(scalari,scalarh,scalarh));
  OUTPUT(clamp(scalarh,foo,foo));
  OUTPUT(clamp(scalarh,foo,foo.x));
  OUTPUT(clamp(scalarh,foo,foo.wzxy));
  OUTPUT(clamp(scalarh,foo,5));
  OUTPUT(clamp(scalarh,foo,5.5f));
  OUTPUT(clamp(scalarh,foo,5.7));
  OUTPUT(clamp(scalarh,foo,scalarf));
  OUTPUT(clamp(scalarh,foo,scalari));
  OUTPUT(clamp(scalarh,foo,scalarh));
  OUTPUT(clamp(scalarh,foo.x,foo));
  OUTPUT(clamp(scalarh,foo.x,foo.x));
  OUTPUT(clamp(scalarh,foo.x,foo.wzxy));
  OUTPUT(clamp(scalarh,foo.x,5));
  OUTPUT(clamp(scalarh,foo.x,5.5f));
  OUTPUT(clamp(scalarh,foo.x,5.7));
  OUTPUT(clamp(scalarh,foo.x,scalarf));
  OUTPUT(clamp(scalarh,foo.x,scalari));
  OUTPUT(clamp(scalarh,foo.x,scalarh));
  OUTPUT(clamp(scalarh,foo.wzxy,foo));
  OUTPUT(clamp(scalarh,foo.wzxy,foo.x));
  OUTPUT(clamp(scalarh,foo.wzxy,foo.wzxy));
  OUTPUT(clamp(scalarh,foo.wzxy,5));
  OUTPUT(clamp(scalarh,foo.wzxy,5.5f));
  OUTPUT(clamp(scalarh,foo.wzxy,5.7));
  OUTPUT(clamp(scalarh,foo.wzxy,scalarf));
  OUTPUT(clamp(scalarh,foo.wzxy,scalari));
  clamp(foo,foo,foo);
  clamp(foo,foo.wzxy,foo);
  clamp(scalarh,foo.wzxy,foo);
  clamp(scalarh,foo.wzxy,scalarh);
  OUTPUT(clamp(scalarh,foo.wzxy,scalarh));
  OUTPUT(clamp(scalarh,5,foo));
  OUTPUT(clamp(scalarh,5,foo.x));
  OUTPUT(clamp(scalarh,5,foo.wzxy));
  OUTPUT(clamp(scalarh,5,5));
  OUTPUT(clamp(scalarh,5,5.5f));
  OUTPUT(clamp(scalarh,5,5.7));
  OUTPUT(clamp(scalarh,5,scalarf));
  OUTPUT(clamp(scalarh,5,scalari));
  OUTPUT(clamp(scalarh,5,scalarh));
  OUTPUT(clamp(scalarh,5.5f,foo));
  OUTPUT(clamp(scalarh,5.5f,foo.x));
  OUTPUT(clamp(scalarh,5.5f,foo.wzxy));
  OUTPUT(clamp(scalarh,5.5f,5));
  OUTPUT(clamp(scalarh,5.5f,5.5f));
  OUTPUT(clamp(scalarh,5.5f,5.7));
  OUTPUT(clamp(scalarh,5.5f,scalarf));
  OUTPUT(clamp(scalarh,5.5f,scalari));
  OUTPUT(clamp(scalarh,5.5f,scalarh));
  OUTPUT(clamp(scalarh,5.7,foo));
  OUTPUT(clamp(scalarh,5.7,foo.x));
  OUTPUT(clamp(scalarh,5.7,foo.wzxy));
  OUTPUT(clamp(scalarh,5.7,5));
  OUTPUT(clamp(scalarh,5.7,5.5f));
  OUTPUT(clamp(scalarh,5.7,5.7));
  OUTPUT(clamp(scalarh,5.7,scalarf));
  OUTPUT(clamp(scalarh,5.7,scalari));
  OUTPUT(clamp(scalarh,5.7,scalarh));
  OUTPUT(clamp(scalarh,scalarf,foo));
  OUTPUT(clamp(scalarh,scalarf,foo.x));
  OUTPUT(clamp(scalarh,scalarf,foo.wzxy));
  OUTPUT(clamp(scalarh,scalarf,5));
  OUTPUT(clamp(scalarh,scalarf,5.5f));
  OUTPUT(clamp(scalarh,scalarf,5.7));
  OUTPUT(clamp(scalarh,scalarf,scalarf));
  OUTPUT(clamp(scalarh,scalarf,scalari));
  OUTPUT(clamp(scalarh,scalarf,scalarh));
  OUTPUT(clamp(scalarh,scalari,foo));
  OUTPUT(clamp(scalarh,scalari,foo.x));
  OUTPUT(clamp(scalarh,scalari,foo.wzxy));
  OUTPUT(clamp(scalarh,scalari,5));
  OUTPUT(clamp(scalarh,scalari,5.5f));
  OUTPUT(clamp(scalarh,scalari,5.7));
  OUTPUT(clamp(scalarh,scalari,scalarf));
  OUTPUT(clamp(scalarh,scalari,scalari));
  OUTPUT(clamp(scalarh,scalari,scalarh));
  OUTPUT(clamp(scalarh,scalarh,foo));
  OUTPUT(clamp(scalarh,scalarh,foo.x));
  OUTPUT(clamp(scalarh,scalarh,foo.wzxy));
  OUTPUT(clamp(scalarh,scalarh,5));
  OUTPUT(clamp(scalarh,scalarh,5.5f));
  OUTPUT(clamp(scalarh,scalarh,5.7));
  OUTPUT(clamp(scalarh,scalarh,scalarf));
  OUTPUT(clamp(scalarh,scalarh,scalari));
  OUTPUT(clamp(scalarh,scalarh,scalarh));
  OUTPUT(clamp(float4(-1,2,5.4,10),1.1,7));

  float4 a = clamp(float1(3.14159f), float4(1,2,3,4), float4(2,3,4,5));
  float4 aa = clamp(float1(3.14159f), float4(1,2,3,4), float4(2,3,4,5));

  OUTPUT(clamp(3.14159f, float4(1,2,3,4), float4(2,3,4,5)));
#endif
}
